# deploy/skills/n8n-credentials/

n8n credential types and management — REST API credentials, HTTP Request node authentication, predefinedCredentialType vs genericCredentialType, httpCustomAuth JSON format, credential creation via API, and service-specific schemas. Full documentation in `SKILL.md`.

**Invoke:** `/n8n-credentials` or describe an n8n credential or HTTP Request auth task.
